# Legal Document Enhancement Requirements
# Full capabilities for advanced document processing and semantic chunking

# Core dependencies for advanced document processing and semantic chunking
torch>=2.0.0
transformers>=4.38.0
sentence-transformers>=2.2.2
paddleocr>=2.6.1.3
paddlepaddle>=2.5.2 # CPU version, can be replaced with paddlepaddle-gpu if needed
pdf2image>=1.16.3
pytesseract>=0.3.10
Pillow>=10.2.0
camelot-py[cv]>=0.11.0 # [cv] for OpenCV dependency
numpy>=1.26.4
pandas>=2.2.1
langchain-community>=0.3.29 # For document loaders
langchain-core>=0.3.76
langchain>=0.3.27

# Email processing
# Note: Email processing uses built-in Python libraries (email, quopri, base64)
# No additional dependencies required for MSG/EML processing

# Optional: PST file processing (safe to install)
libpff-python>=20231205  # PST file support - tested compatible

# Archive processing (ZIP/RAR support)
rarfile>=4.0  # RAR file support - optional dependency

# Optional: Advanced OCR
# layoutparser>=0.3.4
# detectron2>=0.6